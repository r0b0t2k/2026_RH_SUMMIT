# Session Source Bundle
Source directory: /home/robot/gitrepos/2026_RH_SUMMIT/otter_summaries/2026_11/guess_and_check_speculative_decoding_accelerates_inference

## Transcript: speculative_decoding.txt
____Header data____
This talk occured on 2026_05_11 at 4:40PM to 5:00PM
____/Header data____

Speaker 1  0:00  
Um, hi, I'm going to try to work out what distance to hold the mic. Okay. My name is finchman owns. This is Helen zo. We're going to be talking about speculative decoding today and how you can use the speculators library to create new speculative decoding models and serve them efficiently in VLM. So first off, what is spec decoding? Spec decoding is this technique that allows you to speed up LLM inference by combining a your large language model that the model you want to serve with a smaller draft model, and then you can get the draft model to produce draft which the large model verifies. And through this combination, you can kind of speed up decoding, getting some of the speed of the draft model, which is much smaller, while still getting the same performance or quality of output as you would get from the verifier the larger model. And the other thing I want to call out really quickly is that this is lossless, and so what we mean by that is that there's no we can prove mathematically that the quality of the final output is going to be just as good if you use the spec decoding model in combination with the large model, as if you had just used the large model, same quality. So what this looks like is something a bit like this. So on the top here, we have kind of the regular decoding process, and on the bottom, spec decoding. So I'm just going to walk through this typically with large language models. The way it works is you take as inputs, tokens, just your words, your sentences, and you feed all of that into the large language model. You do a whole forward pass of the model, and you generate one token as output. And then you take that one token, you feed it right back into the model from the start, and you generate your second token. And then you just keep repeating this process, one token at a time, until you finish your response. And this certainly works. This is how you know all these models are served today, or all of them are served today, is one token at a time. But the challenge here is these large language models are very big, and we want to try to make that run a bit faster. And so that's respect to coding comes in. So it looks a little bit similar to start we have, instead of our super large language model, we have a draft model. This draft model is often an order of magnitude times smaller, if not more, and so it runs a lot faster. And so we we use the same inputs to the draft model, and then we generate outputs. It gets fed right back into the draft and so on. We repeat. But what we're producing here is not the final output of the overall combination. It's just a draft. So the tokens I have listed in gray, and then we take this whole draft, and we feed the draft in to the verifier model, all in parallel. And the verifier model processes the tokens in parallel, and then for each of them, we do this rejection sampling technique. So that means we go one token in time, and we say, Yep, the verifier agrees with the drafter on this token. It agrees on this token, and then we reach the third token, in this case, pi, and the verifier model says, No, I think cake was actually a better answer here. And by doing this process, by doing this rejection sampling, that's how we get this guarantee that the verifier model or that we're producing output that is the quality and as what the verifier model would produce, but it's much faster, and the reason it's faster is because we're doing one forward pass to the verifier instead of the three you see at the top. And now you might be wondering, with a Jacob model, that's as small as I said it is here, you know, surely we don't get a good result here. Surely, you know, the draft model produces kind of garbage, and then the verifier just has to reject all the tokens, and then we don't get any speed up. But the way we actually counteract this, the way we're able to get these draft models to get kind of good outputs that align well with the verifier is by using some tricks so that their small architecture actually isn't a hindrance to kind of the quality of the output. And so I'm going to talk a little bit about that on this slide. So there, there are kind of a few different algorithms that I want to highlight here that kind of introduced a few of the ideas that go into this. So the first one I want to talk about is the eagle three algorithm. And this is on the first algorithms to kind of take insight from the large language model, you know, multiple different states, these hidden states, and feed it into the drafter and and so to explain what that all means, hidden states and all that, usually when we're running our large language model, we're feeding our tokens in here at the bottom, and those go through multiple layers of processing, like through the large language model, one layer at a time. And as it gets processed through the model, we're kind of refining the internal representation that the model has of the tokens and of the context and the state, and we're using that so that by the time we get to the last layer, we're ready to predict what the next next word in the sequence should be. But in this case, these intermediate representations also have a lot of feature rich information about kind of what is the model thinking, what is it planning ahead? And so by taking those outputs from those intermediate layers and feeding that into the draft model alongside the actual token output from the verifier, we're able to get kind of a lot more information for the drafter, which allows it to kind of perform a lot better than it would, you know, regularly for a model of its size, and also to be very aligned in its output with the verifier model. And so thus, the people three algorithms, one of their contributions, and then with that draft model, we're still kind of taking multiple auto recursive steps, so that this is little arrow you see, looping around the draft model to generate one token at a time, but then P goal, the next algorithm I want to talk about, kind of takes this to the next step, next stage, which is, instead of doing one token at a time from the drafter, why don't we parallelize that as well? So what if we use some kind of masking techniques to generate a few tokens at once. And this isn't something we use super often for large language models, because there's a concern that, okay, this might hurt performance a little bit, but for the drafter, we don't care as much, because the performance benefit is okay for a small drop in the performance of the drafter, because at the end of the day, the quality is being guaranteed by the verifier model itself, and not by the drafter. And so it's okay, if we take a small thing there, and the result of that is, instead of doing multiple forward passes through our draft model, we're instead doing a single forward pass through the drafters of speeding up that part of the process as well. And then D Flash has a similar kind of idea to it. So we're also doing parallel token prediction with the draft model, but we've kind of changed the architecture a little bit so that we're generating those tokens kind of all as a block that where each token can see the other tokens in the blocks, and this kind of allows it to get a little bit more information, and allows us to get kind of better quality outputs for the draft. And there's a lot more details on those that I can't share today, but if you have any questions, please feel free to escalate it. But now I want to talk a little bit about the performance of these drafters. So how good is this approach? And here, this is, I mean, a simple model. It's a Quentin three AP model, but we can see the base model on its own in gray, and then a D flash, kind of augmented, speculative decoding version of the same model, and you can see that we're getting, you know, 3x the output. This is measured in inter token latency, so the time between each token that's being generated that's three times faster for this combination. And this is running on the exact same hardware, and the output is, you know, exactly the same quality as if you just use the verifier itself, but, you know, three times faster. And the other cool thing I want to talk about is on the next slide you, some of you might have been here for the previous talk on quantization. And a question we get a lot is, Okay, should I use quantization to speed on my model, or should I use spec decoding, which is better? And so on. And our answer to that is you can actually use both. So these are kind of orthogonal approaches to optimizing these models. And in this case, we're not getting a huge speed up, because I'm just using an FBA dynamic version of the base model, and so it's not very heavily quantized, but there is a still some speed up here, and this can be even more extreme for larger models. So yeah, with with that, I'm going to hand it off to Helen, who's going to talk a little bit about speculators, a library for training these models and also how you can serve them feel and with a quick

Unknown Speaker  9:47  
demo. So at this point of the presentation, you're probably wondering, how do I get on board? How do I train my own speculative coding algorithm? Well, that's why I want to introduce our own library speculators. It's an end to end, unified library to help you convert or train a spider coding model that's directly serveable In BLM, which is a really fast and efficient engine server for serving your large language model. And there are some really nice features about our library I want to introduce today. First, is built in BLM data generation, we get hidden states directly from BLM. These are very important in our training process to help the draft model learn from the verifier model. And similarly, the distributed training, which helps speed up our training process so you can get a really fast model even faster. And most importantly, we support three different state of the art algorithm as finches introduced Eagle 3p eagle and D flash. As new algorithms come out, we'll strive to support them as well. And now, after you've trained a model, we have a guide, LM evaluation support within our repository, and the graphs that you've seen, the nice visualizations are also generated using the system, so hopefully it'll help you visualize how much of a speed up that you're gaining from the speculators model that you just trained. And last but not least, we have seamless VLM inference support. So after you've trained your model, it's very easy to serve in VLM. We want to make the whole process as painless and as effortless as possible. And now, how do we actually get there? I want to take you on a data journey. For example, if we want to train speculators model for deep seq before, think of deep sleep before. As a teacher, we want to generate a bunch of responses from interactions between human and deep seq before. So our draft model actually have some materials to learn from. Think of the verifier as a teacher and draft model as a student. We want the student to eventually be as great as the teacher, to help with the speed up. And then we tokenize and format these responses in a way that large language model can understand and machine can understand. And then we feed it into the infant server, VLM, and actually get the hidden states. Think of them as inner thought of the teacher to help with the training process. And after training, now you've had a speculative coding model. How do we actually use it? It's just one line. It's very simple. Just serve the model. Here. I use an example from the models that we've trained. We put on hugging face. Everything is open source under the Red Hat AI organization. You can also point it to, if you trained your model locally, pointed to a local directory. It's very easy. You now it's the most important and exciting time for the stem. We're doing a demo live. We We made this nice little playground, sort of to visualize and quantize a bunch of benchmarking and help you visualize how much of a speed up that you're gaining from using specular decoding. Here on the left hand side, we're search. We are serving vanilla quant three, 8b model. And on the right hand side, we're serving our trained Red Hat, 138, B, speculator, D, Flash model. Before we actually get started, I want to go through some important metrics we talked about, inter token latency is the time tick from token to token. So the more time it takes, the slower the response would, it would be because speculative coding would generate multiple tokens at the same time, so it will actually speed up into token latency by a lot. Similarly, throughput is about how many tokens that we're generating per second, so more tokens, so more tokens generated means the faster the response that you're going to see for an AI agent. And these are all made possible because these models have high acceptance rate, which means your draft model think very closely as the verifier model, so the proposed tokens are very likely to get accepted, and therefore the speed up we're seeing. Now let's actually try. I'm sending it a prompt to both models, and I'm going to hit send, and you're going to see both side, two different versions generating. We can already see on the right hand side, our model is a lot faster. It, in fact, has already finished. On the top, you can see these little, nice groups that we help visualizing. For example, in one step, two tokens are accepted, five tokens are accepted. You can see these singletons are being left out because they're rejected to help preserve the quality of our response. And on top, we can see, now, on the left hand side, it just finished, we are seeing about 2.2 times speed up end to end the whole process. So if you are serving a model, using it as like a chatbot agent or something, your users will be very happy, because much sooner, instead of just seeing a bubble, and we're seeing 1.7 times inter token latency speed up, and two times throughput speed up. So it's really fast, as you just saw. And these are all made possible because we have a 65% acceptance rate, which means 65% of the token generated by the draft model are being accepted, which is pretty good. And last thing I want to point out is we're seeing this, this speculative decoding algorithm is supposed to be lossless. Sometimes you might see a different response variance, but that's just because we're sampling from possibility distribution, but but the overall quality of the response is still the same. You can verify by benchmarking both models, they should get very similar results, and we're like not sacrificing any quality by gaining this huge speed up. That's it for today. If you want to get involved, please check out our repository and start it if you like. We also are on the Slack channels. We're very active, and we have sync meetings weekly on Thursday at 3:30pm if you want to join and learn more about spectrum decoding and how it works and our progress. We also have a very nice hugging face collection. All the open source models that we've trained are on there. Please try them out. Let us know if you like them. The Red Hat AI team will also want to hear from you. Let us know your thoughts about our presentation today, or sign up to parts.

## PDF Text
No PDF files found in this talk folder.
